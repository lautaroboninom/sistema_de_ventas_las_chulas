# --- STAGING (MySQL) ---

# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=false
ALLOWED_ORIGINS=
FRONTEND_ORIGIN=
PUBLIC_WEB_URL=
TZ=America/Argentina/Buenos_Aires

# MySQL (staging)
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=servicio_tecnico
MYSQL_USER=sepid
MYSQL_PASSWORD=supersegura
# Nota: definir MYSQL_ROOT_PASSWORD solo fuera del repo

# Frontend (build-time)
# Si se desea forzar URL del API en tiempo de build (por fuera del mismo origen):
# VITE_API_URL=http://api-staging:8000

# Presupuestos (PDFs) - opcional
QUOTES_HOST_DIR=./quotes

